/**
 * FeatureC — part definition for Feature C (stub).
 *
 * LEAF FILE.  Globally unique name "FeatureC".
 * Assembled into  Core::Architecture  by the namespace hub.
 */
package FeatureC {
    part def FeatureC_Component {
        doc /* Feature C structural component — extend with ports and attributes as needed. */
    }
}
